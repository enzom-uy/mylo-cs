import SearchInput from '../search-input'

const SearchSection = () => {
    return (
        <section className="w-full pt-6">
            <SearchInput />
        </section>
    )
}

export default SearchSection
